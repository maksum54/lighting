# Optional server-side AI assistant configuration
# Copy to .env (never commit .env) before starting the local server.
AI_API_URL=https://api.openai.com/v1/chat/completions
AI_API_KEY=
AI_MODEL=
# Optional tuning (all have safe defaults):
# AI_SYSTEM_PROMPT=Kamu adalah konsultan lighting...
# AI_MAX_CHARS=800
PORT=8787
